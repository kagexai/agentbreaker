# findings/partial/

Git-tracked library of **partial-success** attack payloads.

**Storage threshold**: `vulnerability_score >= 3` AND `vulnerability_score < 7`

Files named `ATK-XXXXX.yaml`. See `program.md` Section 9 for the YAML schema.

Partial successes are valuable mutation seeds -- they show the guardrail is
under pressure. Build on them with Tier 2-3 mutations from program.md Section 7.
