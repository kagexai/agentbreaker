# findings/success/

Git-tracked library of **full-success** attack payloads.

**Storage threshold**: `vulnerability_score >= 7` OR `breach_detected = true`

Files named `ATK-XXXXX.yaml`. See `program.md` Section 9 for the YAML schema.

Commit format:
```
finding: ATK-XXXXX [LLM0X] <category> <technique> (vuln=X.X)
```
